#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry point of the program
 * print the alphabet in reverse order
 * Return: always 0 (Success)
 */
int main(void)
{
int i:
for (i = 'z'; i >= 'a'; i--)
putchar(i);
putchar('\n');
return (0);
}
