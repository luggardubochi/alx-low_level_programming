#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry point of the program
 * print the alphabet in reverse order
 * Return: always 0 (Success)
 */
int main(void)
{
int ch:
for (ch = 'z'; ch >= 'a'; ch--)
putchar(ch);
putchar('\n');
return (0);
}
