#include <stdio.h>
/**
 * main - print all single digit numbers less than 10
 * Return: Zero if code runs without exception
 */
int main(void)
{
int i;
for (1=0; 1 < 10; 1 = 1 + 1)
{
printf("%d", i)
}
putchar('\n');
return (0);
}
