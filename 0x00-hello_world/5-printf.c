#include <stdio.h>
/**
 * main - Entry point
 * Decription: prints "with proper grammer, but the outcome is a piece of"
 * art,"
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome i a piece of art,\n");
return (0);
}
